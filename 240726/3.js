// 복습 요망

function repeatWord(str, n) {
  for (i = 0; i < n; i++) {
    console.log(str);
  }
}

// 함수 실행
repeatWord("hello", 5); // console.log 포함시키면 undefined값도 출력됨

// 문자열(str)과 숫자(n)을 매개변수로 받아
// str을 n번 콘솔에 출력하는 함수인 repeatWord 함수를 완성해보세요.
// return값이 없으면 undefined
